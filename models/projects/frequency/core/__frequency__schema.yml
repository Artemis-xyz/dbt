# This file is auto-generated from the global schema definitions.
# To override a column definition, add the 'override' tag to that column.

version: 2

column_definitions:
  chain_dau: &chain_dau
    name: chain_dau
    description: "Daily unique users on a chain"

  chain_txns: &chain_txns
    name: chain_txns
    description: "Daily transactions on a chain"

models:
  - name: ez_frequency_metrics_by_chain
    description: "This table stores metrics for the FREQUENCY protocol"
    columns:
      - *chain_dau
      - *chain_txns
  - name: ez_frequency_metrics
    description: "This table stores metrics for the FREQUENCY protocol"
    columns:
      - *chain_dau
      - *chain_txns
