# This file is auto-generated from the global schema definitions.
# To override a column definition, add the 'override' tag to that column.

version: 2

column_definitions:
  buybacks: &buybacks
    name: buybacks
    description: "The amount tokens bought back by the protocol (USD)."
    tags:
      - artemis_gaap

  gross_protocol_revenue: &gross_protocol_revenue
    name: gross_protocol_revenue
    description: "The total value generated by a protocol from all user-paid fees, encompassing al products and services prior to the split with network participants."
    tags:
      - artemis_gaap

  perp_dau: &perp_dau
    name: perp_dau
    description: "The number of unique traders on a perpetuals exchange"

  perp_volume: &perp_volume
    name: perp_volume
    description: "The total trading volume on a perpetuals exchange"

  txn_fees: &txn_fees
    name: txn_fees
    description: "Transaction Fees paid to the dydx chain"

  trading_fees: &trading_fees
    name: trading_fees
    description: "Trading Fees paid by traders on the dydx"

models:
  - name: ez_dydx_metrics_by_version
    description: "This table stores metrics for the DYDX protocol"
    columns:
      - *perp_dau
      - *perp_volume

  - name: ez_dydx_metrics
    description: "This table stores metrics for the DYDX protocol"
    columns:
      - *buybacks
      - *gross_protocol_revenue
      - *perp_dau
      - *perp_volume
      - *txn_fees
      - *trading_fees

  - name: ez_dydx_metrics_by_chain
    description: "This table stores metrics for the DYDX protocol"
    columns:
      - *perp_dau
      - *perp_volume
