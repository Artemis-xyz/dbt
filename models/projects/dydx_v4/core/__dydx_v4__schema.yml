# This file is auto-generated from the global schema definitions.
# To override a column definition, add the 'override' tag to that column.

version: 2

column_definitions:
  buybacks: &buybacks
    name: buybacks
    description: "The amount tokens actually bought back by the protocol (USD)."

  fees: &fees
    name: fees
    description: "The total fees generated by a protocol"

  perp_dau: &perp_dau
    name: perp_dau
    description: "The number of unique traders on a perpetuals exchange"

  perp_volume: &perp_volume
    name: perp_volume
    description: "The total trading volume on a perpetuals exchange"

models:
  - name: ez_dydx_v4_metrics_by_chain
    description: "This table stores metrics for the DYDX_V4 protocol"
    columns:
      - *buybacks
      - *fees
      - *perp_dau
      - *perp_volume

  - name: ez_dydx_v4_metrics
    description: "This table stores metrics for the DYDX_V4 protocol"
    columns:
      - *buybacks
      - *fees
      - *perp_dau
      - *perp_volume

