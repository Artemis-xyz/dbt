# This file is auto-generated from the global schema definitions.
# To override a column definition, add the 'override' tag to that column.

version: 2

column_definitions:
  fdmc: &fdmc
    name: fdmc
    description: "The fully diluted market cap of a token in USD"
    tags:
      - artemis_gaap

  market_cap: &market_cap
    name: market_cap
    description: "The market cap of a token in USD"
    tags:
      - artemis_gaap

  price: &price
    name: price
    description: "The price of a token in USD"
    tags:
      - artemis_gaap

  token_turnover_circulating: &token_turnover_circulating
    name: token_turnover_circulating
    description: "The turnover of a token in USD, based on the token's circulating supply."
    tags:
      - artemis_gaap

  token_turnover_fdv: &token_turnover_fdv
    name: token_turnover_fdv
    description: "The turnover of a token in USD, based on the token's fully diluted supply."
    tags:
      - artemis_gaap

  token_volume: &token_volume
    name: token_volume
    description: "The volume of a token in USD"
    tags:
      - artemis_gaap

  tvl: &tvl
    name: tvl
    description: "The total value in USD restaked using Eigenpie restaking protocol"
    tags:
      - override

  tvl_native: &tvl_native
    name: tvl_native
    description: "The total value in native currency restaked using Eigenpie restaking protocol"
    tags:
      - override

models:
  - name: ez_eigenpie_metrics
    description: "This table stores metrics for the EIGENPIE protocol"
    columns:
      - *fdmc
      - *market_cap
      - *price
      - *token_turnover_circulating
      - *token_turnover_fdv
      - *token_volume
      - *tvl
      - *tvl_native

    tests:
      - not_null:
          column_name: date
          description: "Ensures that the date column is not null"
          severity: error
      - dbt_utils.recency:
          datepart: day
          interval: 2
          field: date
          description: "Ensures that the date column contains at least one record from the last 2 days"
          severity: error
      - dbt_utils.expression_is_true:
          expression: date = cast(date as date)
          description: "Ensures that the date column is properly typed as DATE with yyyy-mm-dd format"
          severity: error
  - name: ez_eigenpie_metrics_by_chain
    description: "This table stores metrics for the EIGENPIE protocol"
    columns:
      - *tvl
      - *tvl_native

    tests:
      - not_null:
          column_name: date
          description: "Ensures that the date column is not null"
          severity: error
      - dbt_utils.recency:
          datepart: day
          interval: 2
          field: date
          description: "Ensures that the date column contains at least one record from the last 2 days"
          severity: error
      - dbt_utils.expression_is_true:
          expression: date = cast(date as date)
          description: "Ensures that the date column is properly typed as DATE with yyyy-mm-dd format"
          severity: error
      - not_null:
          column_name: chain
          description: "Ensures that the chain column is not null for by_chain tables"
          severity: error
