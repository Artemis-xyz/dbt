# This file is auto-generated from the global schema definitions.
# To override a column definition, add the 'override' tag to that column.

version: 2

column_definitions:
  tvl: &tvl
    name: tvl
    description: "The total value locked in a protocol"

models:
  - name: ez_bitflow_metrics
    description: "This table stores metrics for the BITFLOW protocol"
    columns:
      - *tvl
    tests:
      - dbt_utils.recency:
          datepart: day
          interval: 2
          field: date
          description: Ensures that the date column contains at least one record from the last 2 days
      - not_null:
          column_name: date
          description: Ensures that the date column is not null
      - dbt_utils.expression_is_true:
          expression: "date = cast(date as date)"
          description: Ensures that the date column is properly typed as DATE with yyyy-mm-dd format
  - name: ez_bitflow_metrics_by_chain
    description: "This table stores metrics for the BITFLOW protocol"
    columns:
      - *tvl
    tests:
      - not_null:
          column_name: chain
          description: Ensures that the chain column is not null
      - dbt_utils.recency:
          datepart: day
          interval: 2
          field: date
          description: Ensures that the date column contains at least one record from the last 2 days
      - not_null:
          column_name: date
          description: Ensures that the date column is not null
      - dbt_utils.expression_is_true:
          expression: "date = cast(date as date)"
          description: Ensures that the date column is properly typed as DATE with yyyy-mm-dd format
