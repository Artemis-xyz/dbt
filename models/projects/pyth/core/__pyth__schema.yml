# This file is auto-generated from the global schema definitions.
# To override a column definition, add the 'override' tag to that column.

version: 2

column_definitions:
  gross_protocol_revenue: &gross_protocol_revenue
    name: gross_protocol_revenue
    description: "The total USD value generated by a protocol from all user-paid fees, encompassing all products and services prior to the split with network participants."
    tags:
      - artemis_gaap

  oracle_dau: &oracle_dau
    name: oracle_dau
    description: "The number of daily active addresses using Pyth."
    tags:
      - override

  oracle_txns: &oracle_txns
    name: oracle_txns
    description: "The number of transactions done through Pyth."
    tags:
      - override

models:
  - name: ez_pyth_metrics
    description: "This table stores metrics for the PYTH protocol"
    columns:
      - *gross_protocol_revenue
      - *oracle_dau
      - *oracle_txns

