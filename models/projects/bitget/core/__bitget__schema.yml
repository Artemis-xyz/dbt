# This file is auto-generated from the global schema definitions.
# To override a column definition, add the 'override' tag to that column.

version: 2

column_definitions:
  burns_native: &burns_native
    name: burns_native
    description: "The amount of native tokens burned"

  fdmc: &fdmc
    name: fdmc
    description: "The fully diluted market cap of a token in USD"
    tags:
      - artemis_gaap

  market_cap: &market_cap
    name: market_cap
    description: "The market cap of a token in USD"
    tags:
      - artemis_gaap

  price: &price
    name: price
    description: "The price of a token in USD"
    tags:
      - artemis_gaap

  token_volume: &token_volume
    name: token_volume
    description: "The volume of a token in USD"
    tags:
      - artemis_gaap

models:
  - name: ez_bitget_metrics
    description: "This table stores metrics for the BITGET protocol"
    columns:
      - *burns_native
      - *fdmc
      - *market_cap
      - *price
      - *token_volume

  - name: ez_bitget_metrics_by_chain
    description: "This table stores metrics for the BITGET protocol"
    columns:
      - *burns_native
      - *fdmc
      - *market_cap
      - *price
      - *token_volume

