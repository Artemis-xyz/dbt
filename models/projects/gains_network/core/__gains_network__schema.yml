# This file is auto-generated from the global schema definitions.
# To override a column definition, add the 'override' tag to that column.

version: 2

column_definitions:
  buybacks: &buybacks
    name: buybacks
    description: "The amount tokens bought back by the protocol (USD). Buybacks started after a proposal was passed on July 12th 2024 to use 90% that was set out to stakers to burn GNS."
    tags:
      - artemis_gaap

  staking_fee_allocation: &staking_fee_allocation
    name: staking_fee_allocation
    description: "Revenue distributed to users who commit tokens (e.g., stake, lock, vote-escrow) to receive a share of protocol fees. This became zero after a proposal pass on July 12th 2024 to use this to burn GNS instead."
    tags:
      - artemis_gaap

  foundation_fee_allocation: &foundation_fee_allocation
    name: foundation_fee_allocation
    description: "Revenue allocated to a foundation affiliated with the protocol, used for grants, partnerships, operations, or legal."
    tags:
      - artemis_gaap

  ecosystem_revenue: &ecosystem_revenue
    name: ecosystem_revenue
    description: "The total value generated by a protocol from all user-paid fees, encompassing al products and services prior to the split with network participants."
    tags:
      - artemis_gaap

  perp_dau: &perp_dau
    name: perp_dau
    description: "The number of unique traders on a perpetuals exchange"

  perp_volume: &perp_volume
    name: perp_volume
    description: "The total trading volume on a perpetuals exchange"

  service_fee_allocation: &service_fee_allocation
    name: service_fee_allocation
    description: "The share of protocol revenue accrued to service providers of the protocol."
    tags:
      - artemis_gaap

  treasury_fee_allocation: &treasury_fee_allocation
    name: treasury_fee_allocation
    description: "Revenue allocated to the protocol's treasury for future use, including development, growth, or governance."
    tags:
      - artemis_gaap

  tvl: &tvl
    name: tvl
    description: "The total value locked in a protocol"

  tvl: &referral_fees
    name: referral_fees
    description: "Fees paid to referrals"

  tvl: &nft_bot_fees
    name: nft_bot_fees
    description: "Fees paid to NFT Trading bots"

models:
  - name: ez_gains_network_metrics_by_chain
    description: "This table stores metrics for the GAINS_NETWORK protocol"
    columns:
      - *perp_dau
      - *perp_volume

  - name: ez_gains_network_metrics
    description: "This table stores metrics for the GAINS_NETWORK protocol"
    columns:
      - *buybacks
      - *staking_fee_allocation
      - *foundation_fee_allocation
      - *ecosystem_revenue
      - *perp_dau
      - *perp_volume
      - *service_fee_allocation
      - *treasury_fee_allocation
      - *tvl
      - *nft_bot_fees
      - *referral_fees
